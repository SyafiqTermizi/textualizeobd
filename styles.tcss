Screen {
    layout: grid;
    grid-size: 4;
}

Display {
    width: 100%;
    text-align: center;
    height: auto;
    padding: 1 2;
    background: $panel;
    border: wide $background;
    /* align: center middle; */

    & Digits {
        width: auto;
        color: $secondary;
    }
}